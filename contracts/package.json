{
  "name": "sentinel-oracle-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Sentinel Oracle",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.js",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network sepolia"
  },
  "keywords": [
    "oracle",
    "pyth",
    "defi",
    "lit-protocol"
  ],
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "@pythnetwork/pyth-sdk-solidity": "^2.2.0",
    "axios": "^1.6.5",
    "dotenv": "^16.3.1",
    "ethers": "^6.10.0"
  }
}
